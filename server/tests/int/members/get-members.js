const request = require('request-promise-native');
const { BASE_URL } = require('../utils/setup');

describe('Integration::Members::GetMembers', () => {

    test('should return members in array', async () => {
        // Given
        // When


        // Then

    });
});
